CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
CMakeUserPresets.json

build*/
src/kokkos_config.hpp

# Byte-compiled plotting/benchmark scripts
__pycache__/
*.py[cod]

# W4-11 sweep artefacts (resumable run cache + emitted CSVs)
w411_cache/
benchmark_w411_*.csv

spack.lock
spack/cpu/.spack*/

*-rep

# OS / editor cruft
.DS_Store
.Rhistory

# LaTeX build intermediates. Scoped to latex/ on purpose: a bare *.log would
# also swallow the tracked basis-set generation logs under
# input/zorabasis_cholesky/. The emitted PDFs stay tracked.
latex/*.aux
latex/*.bbl
latex/*.bcf
latex/*.blg
latex/*.fdb_latexmk
latex/*.fls
latex/*.lof
latex/*.log
latex/*.lot
latex/*.out
latex/*.run.xml
latex/*.synctex.gz
latex/*.toc


# CLion
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#cmake-build-*
